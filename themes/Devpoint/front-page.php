<?php
/**
 * Front page — used when the site is configured with a static front page.
 * Falls through to the same template as the blog index.
 *
 * @package devpoint
 */
require get_template_directory() . '/home.php';
